<div class="container">
  <div class="row my-3">
    <div class="col-md-3">
      <%= render "users/info", user: @user %>
      <%= render "books/form", book: Book.new %>
    </div>
    <div class="col-md-7 offset-1">
      <%= render "books/book_detail", book: @book %>
      <%= render "books/comment", book: @book, book_comment: @book_comment, book_comments: @book_comments %>
    </div>
  </div>
</div>