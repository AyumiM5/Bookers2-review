<div class="container my-2">
  <div class="row">
    <div class="mx-auto col-md-10 col-lg-8 col-sm-12 px-sm-0">
      <h2>Follower Users</h2>
      <% if @users.exists? %>
        <%= render "users/list", users: @users %>
       <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>